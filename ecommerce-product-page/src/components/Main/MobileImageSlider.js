import React from 'react';
import { itemDetails } from '../../data';

export default function MobileImageSlider({ className }) {
	return (
		<div
			className={`bg-orange-400 h-[75vw] sm:h-[60vw] w-full${className}`}
		></div>
	);
}
